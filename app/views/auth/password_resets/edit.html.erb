<% provide(:title, t_nf("auth/users.title.update_password")) %>
<% provide(:icon_class, 'fas fa-key') %>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6 col-lg-4 mt-3">
      <h5 class="text-center font-weight-light"><%= t_nf("auth/users.hints.update_password") %></h5>
      <%= simple_form_for form do |f| %>
        <%= f.input :password %>
        <%= f.hint t_nf("auth/users.hints.password") %>
        <%= f.input :password_confirmation, required: true %>
        <div class="text-center mb-3">
          <%= f.button :submit, t_nf("auth/users.title.update_password"),  class: 'btn btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
