
  <tr id="company_<%= company.id %>">
    <th scope="row"><%= link_to company.name, company_path(company),  {:remote => true, 'data-toggle' =>  "modal", 'data-target' => '#show-modal' }  %></th>
    <td>
      <%= link_to 'Edit', update_modal_company_path(company), :remote => true, :'data-toggle' =>  "modal", :'data-target' => '#update-modal', :class => 'btn btn-info btn-sm mr-2'  %>
      <%= link_to 'Delete', destroy_modal_company_path(company), :remote => true, :'data-toggle' =>  "modal", :'data-target' => '#destroy-modal', :class => 'btn btn-danger btn-sm mr-2'  %>
    </td>
  </tr>
