$("form").replaceWith("<%=  j render 'companies/new_edit_form', form_type: 'update' %>");
<% if @company.valid? %>
  $("#feedback").html("<%= j render 'shared/modal_save_success' %>");
  $("#company_<%=@company.id%>").replaceWith('<%= j render @company, company: @company %>');
<% end %>
