<% if @dock_request_group.valid? %>
  $("#modal-wrapper").html("<%= j render 'shared/generic_modal', { :form_type => 'update', :title => 'Edit Dock Request Group', :form_path => 'dock_request_groups/new_edit_form' } %>");
  $("#update-modal").modal('show');
<% elsif !@dock_request_group %>
  alert_custom("warning","Dock request group no longer exists")
<% end %>
