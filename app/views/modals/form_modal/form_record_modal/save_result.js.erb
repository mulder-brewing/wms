<% record = modal.form.record %>
<% table = modal.table %>
var modal_id = '<%= modal.id %>';
<% if modal.form.save_success %>
  hideModal(modal_id);
  <% if !table.nil? %>
    function renderRecord() {
      return '<%= j render partial: Page::IndexListPage::RECORD_HTML_PATH, locals: { record: record, table: table } %>'
    }
    <% if modal.role?(:new_create) %>
      $('#<%= table.body_id %>').prepend(renderRecord());
      $('#<%= table.row_id(record) %>').hide().fadeIn(1000);
    <% elsif modal.role?(:edit_update) %>
      $('#<%= table.row_id(record) %>').replaceWith(renderRecord());
    <% end %>
  <% end %>
  alert_custom('<%= Constants::Alert::SUCCESS %>', '<%= t "alert.save.success" %>');
<% else %>
  $("#<%= modal.form.html_id %>").replaceWith("<%=  j render modal.form.view_path, modal: modal %>");
<% end %>
