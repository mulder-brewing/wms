<tr id="<%= table.row_id(record) %>">
  <% table.columns.each do |column| %>
    <td>
      <% if column.actions? && column.buttons? %>
        <% column.buttons.each do |button| %>
          <%= btn(button, record: record) %>
        <% end %>
      <% else %>
        <%= time_or_value(column.field_value(record)) %>
      <% end %>
    </td>
  <% end %>
</tr>
