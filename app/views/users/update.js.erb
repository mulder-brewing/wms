<% if @user.save_success %>
  $('#update-modal').modal('hide');
  $("#user_<%=@user.id%>").replaceWith('<%= j render @user, user: @user %>');
  alert_custom("success", "User updated successfully!")
<% else %>
  $("#update-form").replaceWith("<%=  j render 'users/new_edit_form', form_type: 'update' %>");
  if ( $("div.password").hasClass("form-group-invalid") || $("div.user_password_confirmation").hasClass("form-group-invalid") || $("input#user_send_email").hasClass("is-invalid") ) {
      $("#collapseExample").addClass("show");
  }
<% end %>
